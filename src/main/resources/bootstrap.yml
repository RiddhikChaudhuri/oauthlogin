logging:
  level:
    org.apache.zookeeper.ClientCnxn: WARN


server:
  port: 9080  
      
spring:
  application:
    name: UserRolesConsumer
  cloud:
    zookeeper:
      discovery:
        enabled: true

        

UserRolesConsumer:
  ribbon:
    listOfServers: http://localhost:9080